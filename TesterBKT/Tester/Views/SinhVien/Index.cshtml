@model List<Tester.Models.SinhVien>
@{
    ViewData["Title"] = "Index";
}

<h2>Danh sách sinh viên</h2>
<a href="/SinhVien/Add" class="btn btn-success">Thêm mới sinh viên</a>
<table class="table">
    <thead>
        <tr>
            <th>Mã sinh viên</th>
            <th>Tên sinh viên</th>
            <th>Lớp</th>
            <th>Điểm trung bình</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.MaSv</td>
                <td>@item.TenSv</td>
                <td>@item.Lop</td>
                <td>@item.DiemTb</td>
                <td>
                    <a class="btn btn-success" href="/SinhVien/Edit/@item.MaSv" style="margin-right: 10px">Sửa</a>
                    <a class="btn btn-danger" asp-action="Delete" asp-controller="SinhVien" asp-route-id="@item.MaSv">Xóa</a>
                </td>
            </tr>
        }
    </tbody>
</table>